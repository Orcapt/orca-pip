# env.lambda.example - Environment Variables Template
# ====================================================
#
# Copy this file to `.env.lambda` in your project root and fill in your values.
# NEVER commit .env.lambda to git!
#
# Usage:
# 1. cp examples/env.lambda.example .env.lambda
# 2. Edit .env.lambda with your actual values
# 3. Deploy: orca ship my-agent --image my-agent:latest --env-file .env.lambda

# ============================================================================
# AI Provider Keys (REQUIRED - choose what you need)
# ============================================================================

OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# COHERE_API_KEY=...
# HUGGINGFACE_API_KEY=...

# ============================================================================
# Orca Streaming Configuration (REQUIRED for production)
# ============================================================================

STREAM_URL=https://centrifugo.your-org.com
STREAM_TOKEN=ST_xxx

# ============================================================================
# Database (Optional - if your agent needs persistence)
# ============================================================================

# PostgreSQL
# DB_URL=postgresql+psycopg://user:pass@host:5432/dbname

# MongoDB
# MONGO_URL=mongodb://user:pass@host:27017/dbname

# Redis (for caching/sessions)
# REDIS_URL=redis://user:pass@host:6379/0

# ============================================================================
# Logging & Monitoring (Optional)
# ============================================================================

LOG_LEVEL=info
# LOG_FORMAT=json
# SENTRY_DSN=https://...@sentry.io/...

# ============================================================================
# AI Model Configuration (Optional)
# ============================================================================

# MODEL=gpt-4
# MAX_TOKENS=2000
# TEMPERATURE=0.7
# TOP_P=1.0

# ============================================================================
# Application Settings (Optional)
# ============================================================================

# APP_NAME=my-agent
APP_VERSION=1.0.4
# (Optional: Detected automatically if set, or falls back to SDK version)
# ENVIRONMENT=production

# ============================================================================
# Notes
# ============================================================================
#
# 1. SQS_QUEUE_URL is automatically set by orca-cli during deployment
#    (you don't need to set it manually)
#
# 2. All these variables will be available in your Lambda function via
#    os.environ.get('VARIABLE_NAME')
#
# 3. Keep this file secure! It contains sensitive API keys.
#
# 4. The orca-cli will merge:
#    - Variables from .env.lambda (this file)
#    - Variables passed via --env KEY=value flags
#    - Auto-generated variables (like SQS_QUEUE_URL)

